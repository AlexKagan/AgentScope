"""AgentScope - an experimental agent-design platform.

Phase 0 provides only the stable platform boundaries: configuration, the
architecture registry, the sandbox-runtime contract, and telemetry. It ships no
runnable agent behavior and no concrete sandbox backend.
"""

__version__ = "0.0.0"
